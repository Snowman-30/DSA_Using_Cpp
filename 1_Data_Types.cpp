#include <bits/stdc++.h>
using namespace std;
int main()
{
    // Data types
    /*  1. int          -> Stores all the integer value. (without decimal)
        2. float        -> Stores all the numbes including decimal values.
        3. double       -> Stores large range of number values.
        4. long         -> Stores even large range of number values.
        5. long long    -> Stores large large range of number values.
        6. string       -> Stores words, statements or any other value inside doube quotes " ".
        7. char         -> Stores single character in single quotes ' '.
    */

    /*  String Function     -> Gets everything before any space.
        string s1, s2;
        cin >> s1 >> s2;
        cout << s1 << " " << s2;
    */
    /*  getline function    -> Gets the entire line of string including spaces
        string str;
        getline(cin, str);
        cout << str;
     */
    /*  Char function       -> Storees all 256 characters.
        char ch = 'd';
        // cin >> ch;
        cout << ch;
    */
    return 0;
}